#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../tsx/dist/cli.mjs" "$@"
else
  exec node  "$basedir/../../../tsx/dist/cli.mjs" "$@"
fi
# cmd-shim-target=/Users/kanizfatemaankan/Desktop/Study/CSE/ISD/smart-fridge-project/node_modules/.pnpm/vite@7.3.3_@types+node@25.9.1_jiti@2.7.0_lightningcss@1.32.0_tsx@4.22.4_yaml@2.9.0/node_modules/tsx/dist/cli.mjs
